# Optional locally: without these, data is saved to .data/db.json.
# Required on Vercel (the local file store is not persistent there).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: without it, explanations fall back to a plain template.
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-3.8-flash
